import {
    MapboxStandardConfig,
    defaultConfig
} from "@/interfaces/mapboxStandardConfig";
import { MapboxStyleId } from "@/interfaces/mapboxStyles";

export const INITIAL_ZOOM = 7;
export const DEFAULT_STYLE: MapboxStyleId = 'standard';
export const DEFAULT_CONFIG: MapboxStandardConfig = defaultConfig;
